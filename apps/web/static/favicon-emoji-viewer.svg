<svg
	xmlns="http://www.w3.org/2000/svg"
	width="24"
	height="24"
	viewBox="0 0 24 24"
	fill="none"
	stroke="#000"
	stroke-width="2"
	stroke-linecap="round"
	stroke-linejoin="round"
>
	<!-- face-slightly-smiling, from Lucide (ISC). https://lucide.dev/icons/face-slightly-smiling
	     The same drawing the bar wears on this page once its title has scrolled
	     away — see the `icon` on Emoji Viewer in src/lib/apps.ts. Kept as a file
	     rather than shared with that component, because a favicon carries CSS a
	     component must not: see the note below. -->
	<style>
		@media (prefers-color-scheme: dark) {
			svg {
				stroke: #fff;
			}
		}
	</style>
	<path d="M15 10V9" />
	<path d="M16.472 15a6 6 0 0 1-8.943 0" />
	<path d="M9 10V9" />
	<circle cx="12" cy="12" r="10" />
</svg>
